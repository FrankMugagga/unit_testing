function stringLength(string) {
    
    if(string.length >= 1 && string.lenth <= 10){
        return string.length;
    }
    else {
        return string.length;
    }
}

module.exports = stringLength;